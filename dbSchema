CREATE TABLE builds (
    id SERIAL PRIMARY KEY,
    build_id INTEGER UNIQUE,
    timestamp TIMESTAMP,
    error_count INTEGER,
    warning_count INTEGER,
    trend TEXT
);

CREATE TABLE build_issues (
    id SERIAL PRIMARY KEY,
    build_id INTEGER,
    severity TEXT,
    category TEXT,
    line TEXT,
    is_error BOOLEAN
);